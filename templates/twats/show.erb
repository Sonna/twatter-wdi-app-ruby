<% twat ||= @twat %>
<%= erb :"twats/twat", locals: {
  retwated_by: nil,
  created_at: twat.created_at,
  twat_id: twat.id,
  twatter_id: twat.twatter_id,
  name: twat.twatter.name,
  username: twat.twatter.username,
  image_url: twat.twatter.image_url,
  like: twat.like(current_user.id),
  likes_count: twat.likes_count,
  retwats_count: twat.retwats_count,
  comments_count: twat.comments_count,
  message: twat.message
} %>
